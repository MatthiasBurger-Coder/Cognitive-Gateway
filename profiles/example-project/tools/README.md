# Example Tool Bindings

Reserved for project-specific capability and tool bindings. Bindings identify allowed abstract capabilities; concrete MCP or tool adapters remain outside the deterministic core.

Concrete tool bindings are introduced with the profile and policy slices.
